<Window x:Class="TangyuanDolaStudio.Dialogs.ProfileEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="账号实例"
        Width="420" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#171A22"
        Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="实例名称" FontSize="15" FontWeight="SemiBold"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Margin="0,12,0,0" Height="34"
                 VerticalContentAlignment="Center"
                 Background="#222733" Foreground="White" BorderBrush="#3A4252"
                 KeyDown="NameBox_KeyDown"/>
        <TextBlock Grid.Row="2" Margin="0,10,0,0" Foreground="#8F9AAF" FontSize="11" TextWrapping="Wrap"
                   Text="这个名称只用于本地识别。账号登录仍由你本人在 Dola 页面正常完成。"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Click="Cancel_Click" MinWidth="82" Height="32" Margin="0,0,8,0"/>
            <Button Content="保存" Click="Save_Click" MinWidth="82" Height="32" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
