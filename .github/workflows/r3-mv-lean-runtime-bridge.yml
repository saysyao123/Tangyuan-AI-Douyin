name: Lean R1 MV Runtime Bridge

on:
  push:
    branches:
      - test/mv-lean-r1
    paths:
      - '04_HARNESS/lean_runtime_bridge/requests/*.json'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: mv-lean-runtime-bridge-${{ github.ref }}
  cancel-in-progress: false

jobs:
  execute-lean-runtime-requests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync latest branch truth
        shell: bash
        run: |
          set -euo pipefail
          git pull --rebase origin "$GITHUB_REF_NAME"

      - name: Static compile
        shell: bash
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: |
          set -euo pipefail
          python -B -m py_compile \
            04_HARNESS/tools/mv_runtime_bridge.py \
            04_HARNESS/tools/mv_lean_runtime_bridge.py

      - name: Process pending Lean Runtime requests
        shell: bash
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: |
          set -euo pipefail
          python -B 04_HARNESS/tools/mv_lean_runtime_bridge.py process-pending

      - name: Commit responses and authoritative canonical mutations
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          git add -A -- \
            04_HARNESS/lean_runtime_bridge/responses \
            06_TESTS/MV/WEB_R3/30D_60 \
            05_IP_ASSETS/MV_30D_60_TRACKER.csv

          if ! git diff --cached --quiet -- 04_HARNESS/lean_runtime_bridge/requests; then
            echo 'ERROR: Lean bridge attempted to modify immutable request files.'
            exit 1
          fi

          if git diff --cached --quiet; then
            echo 'No response or canonical mutation to commit.'
            exit 0
          fi

          git commit -m "lean-runtime: process request(s)"
          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "HEAD:$GITHUB_REF_NAME"
