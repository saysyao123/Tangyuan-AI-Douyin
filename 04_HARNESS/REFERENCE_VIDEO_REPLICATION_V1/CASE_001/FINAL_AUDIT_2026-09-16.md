# CASE_001 Final Deep-Replica Audit — 2026-09-16

## Final state

This reference has now been analyzed to production-ready replica-design level.

Verified source facts:

- duration 108.500 s, 1080×1920, 30 fps;
- unique major silence ~2.85–5.13 s (2.28 s), functioning as anticipation before persona reveal;
- hand-occlusion identity reveal roughly 4.7–5.6 s;
- full-frame color-bar pattern interrupt 8.00–8.50 s;
- stable main creator world begins ~8.50 s;
- 65 distinct visible Caption states across the following 100 s;
- mean / median Caption-state duration ~1.54 / 1.60 s;
- ~6.32 visible Chinese characters/s across main Caption states;
- 29 performer optical-flow motion peaks detected; 23/29 fall within ±0.5 s of a Caption-state start;
- at least 10 main-body jump cuts confirmed by fine frame inspection.

## Transferable directing grammar

Question → held pause → identity reveal → pattern interrupt → stable persona world → thesis → distinction → constraint escalation → second proof → rhetorical/absurdity escalation → compact list → binary payoff/punchline.

The main visual engine is not shot count. It is stable camera + phrase-sized Caption handoffs + gesture/performance density + occasional motivated jump cuts.

## Production recommendation

Do not request a single 100+ s generated clip. Use one approved target identity/world authority and several 8–12 s Semantic Takes. Let generated video own performance; let deterministic composition own persistent topic board, Chinese Caption, environmental typography, pattern interrupt, jump cuts/trims and final mix.

When motion matters, reuse short source excerpts as motion evidence while target image/reference owns identity/world.

## Hypit parity

Reference-understanding parity: **CONDITIONAL PASS**.

Passed:

- whole-piece reading;
- system entry/state/persistence/exit;
- source-time Timeline;
- Caption configuration analysis;
- fact vs interpretation separation;
- semantic transfer relationships;
- motion-reference excerpts;
- target replication blueprint.

Single major missing item:

- true acoustic word/character timing (`hypit.transcript@1`-grade evidence).

Current environment does not have Faster-Whisper / WhisperX / Xingyu model weights, so no acoustic timing was fabricated. Burned-in Caption transitions were measured visually at ~0.1 s resolution instead.

## Alignment route to finish parity

Use the already proven Tangyuan route:

1. approve burned-in source captions as candidate trusted text;
2. P1 Faster-Whisper Small + word timestamps + trusted-text mapping;
3. trigger P2 Xingyu CTC only for conflicts over threshold;
4. export final text/start/end units into `hypit.transcript@1`;
5. rebuild word-labeled evidence grids.

Prior project evidence showed P1 could expose a 0.740 s local conflict and P2 reduce final discrepancies to publication-grade ranges (~0.047/0.183 s median/max in one run; ~0.036/0.082 s in another locked run).

## End-to-end status

- REFERENCE_STRUCTURE_ANALYSIS = PASS
- SYSTEM_LIFECYCLE_ANALYSIS = PASS
- VISUAL_CAPTION_TIMELINE = PASS
- PERFORMANCE_BEAT_ANALYSIS = PASS
- MOTION_REFERENCE_EVIDENCE = PASS
- REPLICA_BLUEPRINT = PASS
- HYPIT_REFERENCE_PARITY = CONDITIONAL_PASS
- ACOUSTIC_WORD_ALIGNMENT = PENDING
- END_TO_END_TARGET_REPLICA = PENDING

The next meaningful test is no longer more documentation: run P1/P2 on this source, then produce one new-topic target through generated Takes + deterministic composition and compare the result back to this blueprint.