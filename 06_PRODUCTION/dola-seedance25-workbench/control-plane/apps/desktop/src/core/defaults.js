'use strict';

// Portable V1 convenience default requested for first-run bootstrap only.
// This is intentionally public and therefore NOT a security boundary. The
// desktop UI must clearly recommend changing it after first launch.
const DEFAULT_INITIAL_VAULT_PASSWORD = 'Tangyuan-Portable-2026!';

module.exports = { DEFAULT_INITIAL_VAULT_PASSWORD };
