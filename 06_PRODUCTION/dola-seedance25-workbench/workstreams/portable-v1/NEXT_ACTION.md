# NEXT ACTION

Packaging work is active. The next build commit must use the current `apps/desktop/package.json` blob SHA (the branch has advanced since the earlier snapshot), then add the Windows portable release workflow and artifact ZIP assembly.
