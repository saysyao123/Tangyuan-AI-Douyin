# Build Contract

A data-center refresh must produce a public observed snapshot, merge by `aweme_id`, normalize songs at work level, rank positive cross-account repeats, and emit direct Douyin work evidence.

Missing observations are `UNKNOWN`, never negative evidence.
