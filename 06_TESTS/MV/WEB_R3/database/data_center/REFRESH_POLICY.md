# Refresh Policy

Target cadence: every 15 days.
Each refresh is append/merge by `aweme_id`; existing history is never replaced merely because a later public page does not show it.
Absence in a refresh is `UNKNOWN`, not negative evidence.
